"""Data pipeline framework — generic engine + thin domain layer.

Imported, never installed: add the repository root to ``sys.path`` and
``import framework``. See docs/ and the ADRs under docs/adr/ for the design.
"""
