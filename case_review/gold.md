"""Case-review gold helpers built on generic framework reducers.

The framework owns generic `current_silver_to_gold` reducers over an arbitrary
entity id column. This module owns the case-review defaults, including the
`case_id` column name and the ingest-oriented helper names used by case-review
pipelines.
"""
