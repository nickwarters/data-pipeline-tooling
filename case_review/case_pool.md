"""``CasePool`` — case-review population reads built on framework primitives.

``CasePool`` uses the generic ``Store``, ``Dataset``, ``Filter``,
``SchemaCoercion``, and ``WorkingDayCalendar`` primitives, but the concept and
its ``fetch_available_cases`` retrieval belong to the case-review domain.
"""
