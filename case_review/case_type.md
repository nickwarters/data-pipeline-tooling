"""``CaseType`` / ``Variation`` — declarative case-review domain objects.

These helpers live in the case-review application/domain package, not in the
generic framework. Pipelines import them directly when they need case-review
configuration such as Case Type schemas, Variations, and ``question_bank_id``.
"""

