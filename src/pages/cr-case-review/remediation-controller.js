// @ts-check

/**
 * Owns Issues-tab property assignment and event forwarding.
 */
export class RemediationPanelController {
  /**
   * @param {import('./types.js').CaseReviewShellContext} context
   */
  bind(context) {
    // TODO(issue-198): Attach cr-capture and cr-attribute listeners and forward
    // event details to the view model.
    void context;
  }

  /**
   * @param {import('./types.js').CaseReviewShellContext} context
   */
  update(context) {
    // TODO(issue-198): Assign remediation section props and call update with
    // catalogue, answers, and attributeFailures.
    void context;
  }
}
