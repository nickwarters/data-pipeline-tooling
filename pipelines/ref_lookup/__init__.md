```python
"""Reference lookup pipeline example.

Shows how to build a shared reference lookup table from repeated category fields
on a case-level feed, then map the generated ids back to create narrow, normalised
silver tables.

Run from the repo root::

    python -m pipelines.ref_lookup /tmp/ref-lookup-demo
"""

```
